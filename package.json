{
  "name": "attention-monitoring-app",
  "version": "1.0.0",
  "description": "EEG Attention Monitoring Dashboard with React, Node.js, MongoDB, and Firebase",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": ["eeg", "attention", "monitoring", "react", "mongodb", "firebase"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
